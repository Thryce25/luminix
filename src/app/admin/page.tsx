import AdminPageClient from './AdminPageClient';

export const metadata = {
  title: 'Admin Panel - Luminix',
  description: 'Admin dashboard for managing Luminix data',
};

export default function AdminPage() {
  return <AdminPageClient />;
}
